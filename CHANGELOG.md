# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

> **当前处于 `0.x` 初始开发阶段**：公共接口（数据文件格式、配置结构、存储格式等）尚未稳定，后续版本可能在不提升主版本号的前提下做出调整。首个稳定版本预计在 `1.0.0` 发布。

---

## [0.2.0] - 2026-09-13

新增侧边栏业务状态提醒、配置快照工作期机制与单实例约束，将管理员配置的「双端交互」收为程序内置结构，并修复信息同步判空与多实例并发写配置的问题。

### 新增

- **侧边栏业务状态提醒（管理员端 / 成员端）**
  - 管理员端显示配置发布状态（尚未发布 / 存在未发布改动 / 与远程一致），成员端显示待锁定固化的材料数量
  - 状态随发布、锁定等操作即时刷新
- **配置快照机制改为「工作期」判定，并支持配置快照有效天数**
  - 管理员可设置配置快照有效天数（1–365 天，默认 30 天）
  - 按「工作起点与今天之差」判定：与管理员是否发布新版无关，起点也不随成员后续编辑推进
  - 列表页状态标签改为工作期 / 待固化 / 已锁定 / 已存档并着色；模板页新增「重新开始工作期」（仅待固化且未锁定可见）
- **单实例约束：同一用户数据目录下只允许运行一个程序实例**
  - 重复启动时不再打开第二个窗口，而是唤醒并置前已打开的窗口；无法置前时提示「程序已在运行」
  - 约束范围为用户数据目录（Windows 为 `%APPDATA%\RuleDone`，macOS/Linux 为 `~/.ruledone`），不同用户或数据目录互不影响
  - 基于锁文件（`app.lock`）与本地套接字通知，程序退出、崩溃或系统重启后残留锁自动失效

### 变更

- **管理员配置的「双端交互」分组改为程序内置结构**
  - `fields_definition.json` 移除该分组，其键名、类型、必填与显隐规则改由 `admin_config_builtin_fields.py` 统一维护，不再开放修改
  - 管理员首页表单改由内置字段元数据驱动；内置字段不作为文档占位符，避免凭据被导出到 `.docx`
  - 保存时仅保留当前汇总平台的凭据；同步相关配置项的读取收敛到 `DataManager` 封装

### 修复

- **成员基本信息同步的判空与冲突判定不准确**
  - 统一判空规则，数字 0 与布尔 False 不再被误判为空值
  - 「无」恢复为有效业务值：正常同步，与远程不同时判为冲突并拦截，避免静默覆盖
  - 冲突比较改为去除首尾空格后比较，避免因空格造成的误报
- **多实例并发读写配置导致设置丢失或报「文件格式错误」**
  - 配置为「读-改-写 + 直接覆盖」，并发写会互相覆盖设置，甚至读到半截内容；本次由进程级唯一性从根源规避
- **若干界面与提示优化**
  - 「关于」页补充开发团队与版权信息；锁定确认框补充「锁定后无法解锁」提醒；列表页状态标签支持按状态着色
- **完善若干文档**
  - 完善开发者文档与同步说明文档

---

## [0.1.2] - 2026-08-26

### 修复

- **成员端启动时资源自动更新成功无提示的问题**
  - 成员端启动后自动检查资源时，若确有更新则显示提示弹窗，若无需更新仍保持静默
- **完善若干文档**
  - 完善README中对工具的描述，增加工具关于各种数据的策略说明

---

## [0.1.1] - 2026-08-16

主要修复自动同步流程、远程存储连接及若干 UI 展示问题。

### 修复

- **成员端自动同步流程与交互防护优化**
  - 主窗口的配置、资源自动同步逻辑收束至设置页
  - 同步运行时禁用「手动同步」按钮，避免并发操作
  - 约束资源同步执行顺序在信息同步之前
  - 模板页增加资源同步结果校验
  - 自动同步未完成时锁定首页、模板页的编辑入口
- **修复资源更新后页面仍显示旧结构的问题**
  - 将各页面的组件构建与数据加载统一封装为刷新函数，资源同步结束后统一调用
- **修复阿里云 OSS 测试远程连接时的报错**
  - 测试所用权限与推送时保持一致
  - UI 呈现优先推荐阿里云 OSS 作为远程存储
  - 同步更新相关文档与代码配置
- **修复多处 UI 与状态展示异常**
  - 成员设置页资源同步版本检查的信息正确显示为同步状态成功与否
  - 修复 macOS 端模式选择时的按钮布局问题
  - 修复模式选择时底部任务栏无图标的问题
  - 材料导出后的弹窗中增加「打开所在文件夹」按钮

---

## [0.1.0] - 2026-08-07

首个对外发布版本，标志着项目从内部迭代（v0.0.x 构建）进入可对外使用的初始阶段。

### 新增

- **双角色系统**
  - 支持「党支部管理员」和「发展成员」两种角色模式，权限与页面相互隔离
- **基本信息管理**
  - 管理员端：支部信息、上级党委信息、公共字段配置
  - 成员端：个人基本信息填写与保存
- **模板管理**
  - 内置 6 个覆盖党员发展全流程的示例模板（申请入党 → 积极分子 → 发展对象 → 预备党员接收 → 教育考察与转正 → 归档）
  - 模板按发展阶段分组呈现并附说明
  - 管理员端支持模板字段配置
  - 成员端支持模板字段编辑、锁定等功能
- **文档生成**
  - 基于 Word 模板的占位符自动替换
  - 支持单个和批量导出
  - 智能字段映射
- **配置远程同步**
  - 支持管理员配置导出/导入
  - 支持通过 URL 自动同步配置，启动时自动检查配置更新
  - 配置快照机制：批次内配置变更不影响成员端已填写材料的专有项
- **资源同步与档案定制**
  - 内置示例字段定义、模板及模板配置文件，首次启用时写入用户目录
  - 支持自定义字段定义文件、模板文件与模板配置文件，并同步至成员端
- **成员信息双端同步**
  - 支持飞书多维表格、腾讯智能表格、WPS 多维表格
  - 支持 GitHub 令牌、OSS 子账号凭据管理，限制可访问范围
  - 进度提醒：管理员在线表中填写，成员端启动时自动同步
- **安全特性**
  - 基于 Argon2 + AES-GCM 的数据加密
  - 密码设置与验证
  - 加密数据本地存储与远程传输
- **用户界面**
  - 现代化 UI 设计
  - 侧边导航栏布局与响应式表单组件

### 技术栈

| 类别 | 技术 | 用途 |
|:------:|:------:|:------:|
| 开发语言 | [Python 3.10](https://www.python.org/downloads/) | 桌面应用与业务逻辑开发 |
| GUI 设计 | [PySide6](https://doc.qt.io/qtforpython/) + [PyQtDarkTheme](https://github.com/5yutan5/PyQtDarkTheme) | 界面搭建与主题美化 |
| JSON 存储 | [GitHub API](https://docs.github.com/en/rest) + [阿里云 OSS](https://help.aliyun.com/product/31815.html) | 配置文件的静态资源托管 |
| 信息汇总 | [飞书多维表格 API](https://open.feishu.cn/document/server-docs/docs/bitable-v1/notification) + [腾讯智能表格 API](https://cloud.tencent.com/document/product/1213) + [WPS多维表格 API](https://open.wps.cn/) | 成员信息在线汇总平台 |
| Word 处理 | [python-docx](https://python-docx.readthedocs.io/) + [docxtpl](https://docxtpl.readthedocs.io/) | 模板占位符替换、批量导出文档 |
| 数据加密 | [cryptography](https://cryptography.io/) + [argon2-cffi](https://argon2-cffi.readthedocs.io/) | 本地敏感数据加密存储与密码哈希 |
| 打包分发 | [PyInstaller](https://pyinstaller.org/) + [Inno Setup](https://jrsoftware.org/isinfo.php) | 应用打包与 Windows 安装程序制作 |
